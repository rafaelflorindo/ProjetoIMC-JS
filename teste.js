if(!isNaN(pesoConvertido) || !isNaN(alturaConvertida)){
   if(alturaConvertida > 0){
    //da sequencia no código
   }else{
    alert("Altura não pode ser inferior a 0")
    return
   }
 }else{
    alert("Os valores informados devem ser numéricos")
    return
   }
   
   